---
description: Alias for /status - Instant project summary with minimal latency.
---

## User Input

```text
$ARGUMENTS
```

## Goal

This is an alias for `/status`. Execute the exact same logic as the status command.

## Execution

Invoke the `/status` command with the same arguments:

1. Read and execute all steps from `.claude/commands/status.md`
2. Use the same output format
3. Apply the same performance constraints

## Why This Alias Exists

- Some users prefer "summary" terminology
- Provides flexibility without duplicating logic
- Both commands produce identical output

---

*Redirect to /status*
